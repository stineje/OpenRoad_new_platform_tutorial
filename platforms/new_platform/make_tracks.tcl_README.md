One should use the equivalent file from the sample platform as an example.
The make_tracks.tcl file defines the routing tracks used on this platform.
This must be manually adjusted based on the user’s preference, as well as the underlying technology.

The most typical use-case, and the one that should be implemented unless there are particular concerns, will see these metal tracks match the minimum spacing detailed in the platform’s tech LEF.
