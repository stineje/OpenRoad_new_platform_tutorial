One should use the equivalent file from other platforms as an example
Layer resistance values can be obtained from the original platform.

The set_wire_rc -signal and set_wire_rc -clock lines should point to the layers most likely to be used for signals and clocks, respectively.
